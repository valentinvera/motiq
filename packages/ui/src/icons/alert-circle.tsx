import { AlertCircle as AlertCircleIcon } from "lucide-react"
import type { ComponentProps } from "react"

export const AlertCircle = (props: ComponentProps<typeof AlertCircleIcon>) => (
  <AlertCircleIcon {...props} />
)
