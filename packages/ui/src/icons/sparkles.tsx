import { Sparkles as SparklesIcon } from "lucide-react"
import type { ComponentProps } from "react"

export const Sparkles = (props: ComponentProps<typeof SparklesIcon>) => (
  <SparklesIcon {...props} />
)
