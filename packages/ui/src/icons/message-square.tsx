import { MessageSquare as MessageSquareIcon } from "lucide-react"
import type { ComponentProps } from "react"

export const MessageSquare = (
  props: ComponentProps<typeof MessageSquareIcon>
) => <MessageSquareIcon {...props} />
