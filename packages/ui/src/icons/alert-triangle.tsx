import { AlertTriangle as AlertTriangleIcon } from "lucide-react"
import type { ComponentProps } from "react"

export const AlertTriangle = (
  props: ComponentProps<typeof AlertTriangleIcon>
) => <AlertTriangleIcon {...props} />
