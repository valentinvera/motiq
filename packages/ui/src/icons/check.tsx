import { Check as CheckBase } from "lucide-react"
import type { ComponentProps } from "react"

export const Check = (props: ComponentProps<typeof CheckBase>) => (
  <CheckBase {...props} />
)

export const CheckIcon = (props: ComponentProps<typeof CheckBase>) => (
  <CheckBase {...props} />
)
